model Company {
    id Int @id @default(autoincrement())
    name String @db.VarChar(150)
    siret String @db.Char(14) @unique
    password String
    ceo String?
    employees Employee[]
    cars Car[]
    patients Patient[]
}